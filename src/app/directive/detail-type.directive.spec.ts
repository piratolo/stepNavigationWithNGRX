import { DetailTypeDirective } from './detail-type.directive';

describe('DetailTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailTypeDirective();
    expect(directive).toBeTruthy();
  });
});
